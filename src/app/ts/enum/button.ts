export enum ButtonVariants {
  Default = 'default',
  Destructive = 'destructive',
  Outline = 'outline',
  Secondary = 'secondary',
  Ghost = 'ghost',
  Link = 'link',
}

export enum ButtonSizes {
  Default = 'default',
  Small = 'small',
  Large = 'large',
  Icon = 'icon',
}
